[project]
name = "store-monolith"
version = "1.0.0"
description = "The Store as a modular monolith — Backend Bootcamp, Session 1"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]

[tool.uv]
package = false
